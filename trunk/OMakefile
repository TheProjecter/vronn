.SUBDIRS: src
.PHONY: clean clean-results distclean fft
#.DEFAULT: notes reforme
# Use the byte-code compiler


#OCamlLibrary(BinaryParse,src/BinaryParse)
#OCamlLibrary(fft,src/fft)
#OCamlLibrary(affichage,src/affichage)
#OCamlLibrary(reseaux,src/reseaux)
#OCamlLibrary(params,src/params)


# Build the program
#OCamlProgram(run/notes,src/notes)
#OCamlProgram(run/reforme,src/reforme)
#OCamlProgram(run/regardelesbeauxcoefsdelafft,src/regardelesbeauxcoefsdelafft)
#.DEFAULT: run/notes.opt run/reforme.opt

clean:
  rm -f *~ run/* src/*.o src/*.cm* src/*~ src/*.a

clean-results:
  rm -f results/*

distclean: clean clean-results
  rm -f src/fft.out src/*.omc *.omc

maintainerclean: distclean
